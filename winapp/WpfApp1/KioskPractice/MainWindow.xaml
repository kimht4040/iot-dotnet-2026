<Window x:Class="KioskPractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KioskPractice"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="키오스크 연습" Height="800" Width="1080" WindowStartupLocation="CenterScreen"
        
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="15"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignDefaultFont}">

	<Window.DataContext>
		<local:MainViewModel/>
	</Window.DataContext>

	<Grid Background="#F5F6FA">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="350"/>
		</Grid.ColumnDefinitions>

		<Grid Grid.Column="0" Margin="20">
			<Grid.RowDefinitions>
				<RowDefinition Height="80"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
			
            </StackPanel>

			<ItemsControl Grid.Row="1" ItemsSource="{Binding Products}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<WrapPanel/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<materialDesign:Card Width="220" Margin="10" UniformCornerRadius="10"
											 materialDesign:ElevationAssist.Elevation="Dp3">

							<StackPanel Background="White">
								<Image Source="{Binding ImagePath}" Height="180" Stretch="UniformToFill" />

								<StackPanel Margin="15">
									<TextBlock Text="{Binding Name}" FontSize="22" FontWeight="Bold" Margin="0,0,0,5"/>
									<TextBlock Text="{Binding Price, StringFormat={}{0:N0} 원}" FontSize="18" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
								</StackPanel>

								<Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
										CommandParameter="{Binding}"
										Style="{StaticResource MaterialDesignFlatMidBgButton}"
										Margin="15,0,15,15" Height="40" materialDesign:ButtonAssist.CornerRadius="5">
									<StackPanel Orientation="Horizontal">
										<materialDesign:PackIcon Kind="CartPlus" Width="20" Height="20" VerticalAlignment="Center"/>
										<TextBlock Text="담기" Margin="8,0,0,0" FontSize="16" VerticalAlignment="Center"/>
									</StackPanel>
								</Button>
							</StackPanel>
						</materialDesign:Card>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Grid>

		<Border Grid.Column="1" Background="White" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1,0,0,0">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Text="주문 내역" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

				<ListBox Grid.Row="1" ItemsSource="{Binding Cart}" Margin="0,0,0,20">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal" Margin="0,10" VerticalAlignment="Center">
								<TextBlock Text="{Binding Product.Name}" Width="110" FontSize="18" VerticalAlignment="Center"/>

								<Button Content="-" Width="35" Height="35" FontSize="20" FontWeight="Bold" Background="#E2E8F0"
										Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
										CommandParameter="{Binding}"/>

								<TextBlock Text="{Binding Quantity}" Width="40" FontSize="18" TextAlignment="Center" VerticalAlignment="Center"/>

								<Button Content="+" Width="35" Height="35" FontSize="20" FontWeight="Bold" Background="#E2E8F0"
										Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
										CommandParameter="{Binding}"/>

								<TextBlock Text="{Binding SubTotal, StringFormat={}{0:N0}원}" Width="70" FontSize="15" TextAlignment="Right" VerticalAlignment="Center"/>
							</StackPanel>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>

				<StackPanel Grid.Row="2">
					<Separator Margin="0,10"/>
					<DockPanel Margin="0,0,0,20">
						<TextBlock Text="총 결제금액" FontSize="20" FontWeight="Bold"/>
						<TextBlock Text="{Binding TotalPrice, StringFormat={}{0:N0} 원}" FontSize="24" FontWeight="Bold" Foreground="#E53E3E" HorizontalAlignment="Right"/>
					</DockPanel>
					<Button Content="주문 완료" Height="70" FontSize="24" Background="#3182CE" Foreground="White" FontWeight="Bold"/>
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</Window>