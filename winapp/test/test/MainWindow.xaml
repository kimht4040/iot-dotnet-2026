<Window x:Class="test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Login" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <materialDesign:Card Width="300" Padding="32" UniformCornerRadius="8" 
                             materialDesign:ElevationAssist.Elevation="Dp4" 
                             VerticalAlignment="Center" HorizontalAlignment="Center">

            <StackPanel>
                <materialDesign:PackIcon Kind="AccountCircle" Width="64" Height="64" 
                                         HorizontalAlignment="Center" 
                                         Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                         Margin="0,0,0,16"/>

                <TextBlock Text="환영합니다" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,24"/>

                <TextBox materialDesign:HintAssist.Hint="아이디를 입력하세요" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Margin="0,0,0,16" />

                <PasswordBox materialDesign:HintAssist.Hint="비밀번호" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                             Margin="0,0,0,32" />

                <Button Content="로그인" 
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Margin="0,0,0,12" Height="40" 
                        materialDesign:ButtonAssist.CornerRadius="5"/>

                <Button Content="회원가입" 
                        Style="{StaticResource MaterialDesignFlatButton}" 
                        Foreground="{DynamicResource PrimaryHueMidBrush}" />
            </StackPanel>

        </materialDesign:Card>
    </Grid>
</Window>