<mah:MetroWindow x:Class="WpfSmartHomeSensingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" 
        xmlns:local="clr-namespace:WpfSmartHomeSensingApp"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <iconPacks:PackIconSimpleIcons Kind="EclipseMosquitto" Foreground="White"
                                           Margin="5,7,0,0" Width="24" />
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>

    <DockPanel>
        <!-- StatusBar 구조 수정 -->
        <StatusBar DockPanel.Dock="Bottom" Height="28" Foreground="Black">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="MQTT Fake Smumulator ver1.0" />
                    <Separator Background="Gray" Margin="10,0" />
                    <TextBlock x:Name="SbiStatus" Text="Ready" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Label Content="MQTT Broker IP Address" VerticalAlignment="Center" />
                <TextBox x:Name="TxtMqttBrokerIp" Width="250" Margin="5" VerticalAlignment="Center"
                         mah:TextBoxHelper.Watermark="브로커 아이피를 입력하세요"
                         Text="192.168.0.108" />
                <Button x:Name="BtnConnect" Content="CONNECT" Margin="5" Click="BtnConnect_Click"/>

                <TextBlock x:Name="TxtStatus" Text="disconnected" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Red" FontWeight="Bold"/>
            </StackPanel>

            <RichTextBox Grid.Row="1" x:Name="RtbLog" Margin="10" VerticalScrollBarVisibility="Visible">
                <FlowDocument>
                    <Paragraph>
                        <!-- 로그가 여기에 표시됩니다 -->
                    </Paragraph>
                </FlowDocument>
                <RichTextBox.Resources>
                    <Style TargetType="{x:Type Paragraph}">
                        <Setter Property="Margin" Value="0" />
                    </Style>
                </RichTextBox.Resources>
            </RichTextBox>
        </Grid>
    </DockPanel>
</mah:MetroWindow>